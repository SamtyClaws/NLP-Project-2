# This file should be run from within the data directory.
# Run this file with python dev_validation_splitter.py

# This function takes in the name of a text file using the format specified in 
# the project instructions, and creates new dev.txt and validation.txt files.
def split(train_text_file):
	# TODO: determine how to best divide the training data into dev/validation 
	# sets, then split the train.txt file into dev.txt and validation.txt files
	return
